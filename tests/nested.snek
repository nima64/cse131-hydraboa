(let ((a 2) (b 3))
  (block
    (let ((c (* a b)))
     (block
      (set! a (add1 c))
      (loop
      (if (= a 7)
        (loop 
            (if (< a 5) (break (sub1 a)) (set! a (- a 1)))
        )
        (break a)
      )
      )
     )
    )
    (let ((d (isnum a)))
      (if d a b))
    )
)